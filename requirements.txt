# Desktop app. Transcription is fully offline (faster-whisper).
# The optional transcript summary feature uses Google Gemini (online, on demand).
customtkinter==5.2.2
faster-whisper==1.2.1
soundcard==0.4.6
sounddevice==0.5.5
# Per-application live capture (Windows WASAPI process loopback): pycaw enumerates
# audio sessions, psutil resolves process names, comtypes powers the COM glue.
pycaw==20251023
psutil==7.2.2
numpy==2.4.6
pydub==0.25.1
scipy==1.17.1
noisereduce>=3.0.0
mutagen==1.47.0
pygame==2.6.1

# Optional: transcript summarization (online, only when you press "Resumir").
google-genai
python-dotenv

# Optional: Google STT mode in live tab (same engine as Google Docs voice typing).
SpeechRecognition>=3.17.0
pyaudio>=0.2.14
python-docx

# CV upload in the interview tab (PDF text extraction).
pypdf

# Natural neural speech, with offline SAPI5 fallback on Windows.
edge-tts>=7.2.8
pyttsx3

# Optional knowledge source for Resolver (community client; requires `nlm login`).
notebooklm-mcp-cli==0.9.4

